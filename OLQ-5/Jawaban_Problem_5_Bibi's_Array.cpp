#include <stdio.h>

int main (){
	int T,N;
	scanf("%d", &T);
	int index=1;
	while(T--){
		scanf("%d", &N);
		long long int a[N];
		for (int i=0; i<N; i++){
			scanf("%lld", &a[i]);
		}
	}
}
